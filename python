Operations to perform:
  Unapply all migrations: groceryapp
Running migrations:
  No migrations to apply.
  Your models in app(s): 'groceryapp' have changes that are not yet reflected in a migration, and so won't be applied.
  Run 'manage.py makemigrations' to make new migrations, and then re-run 'manage.py migrate' to apply them.
